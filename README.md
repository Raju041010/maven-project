This is a simple Maven project integrated with Jenkins for automated builds.
The project contains a valid pom.xml file that defines basic Java compilation settings using Java 8.
Once pushed to GitHub, Jenkins automatically pulls the code, runs mvn clean install, and generates build artifacts in the target/ directory
